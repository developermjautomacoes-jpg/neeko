---
import { content } from '../config/content.js';
const { services } = content;
---

<section id="servicos" class="services">
  <div class="container">
    <h2 class="section-title fade-up">{services.sectionTitle}</h2>
    <div class="grid services-grid">
      {services.items.map((service, i) => (
        <div class={`card service-card fade-up delay-${(i % 3) + 1}`}>
          <i data-lucide={service.icon} class="service-icon" aria-hidden="true"></i>
          <h3>{service.title}</h3>
          <p>{service.description}</p>
          <a href="#contato" class="service-link">
            Saiba mais <i data-lucide="arrow-right" aria-hidden="true"></i>
          </a>
        </div>
      ))}
    </div>
  </div>
</section>
